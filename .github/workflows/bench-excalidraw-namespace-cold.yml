name: bench-excalidraw-namespace-cold
on:
  workflow_dispatch:
jobs:
  install:
    runs-on: namespace-profile-asgar
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: yarn install --frozen-lockfile
